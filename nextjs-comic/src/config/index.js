import { LOGIN_ITEMS, MENU_ITEMS, userMenu } from "./HeaderMenuItems";
import routes from "./routes";
import sidebarItems from "./SidebarItems";

const config = {
  routes,
  sidebarItems,
  MENU_ITEMS,
  LOGIN_ITEMS,
  userMenu,
};

export default config;
